name: Hello World with GitHub Actions
on:
  push:
    branches:
      - developer
jobs:
    Hello-World:
      runs-on: ubuntu-latest
      steps:
        - name: Hello World
          run: echo "Hello World"
